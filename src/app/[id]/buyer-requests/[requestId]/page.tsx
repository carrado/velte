"use client";

import { VendorRequestDetail } from "@/components/vendor-requests/VendorRequestDetail";

// One matched Buyer Request and the vendor's offer on it — see
// VendorRequestDetail for the page itself (redesigned 2026-09-24).
export default function Page() {
  return <VendorRequestDetail />;
}
